package ru.maxim.horse.services.utils;

public class PossibleHorsePosition {
    protected int x, y, dist;

    PossibleHorsePosition(int x, int y, int dist) {
        this.x = x;
        this.y = y;
        this.dist = dist;
    }
}